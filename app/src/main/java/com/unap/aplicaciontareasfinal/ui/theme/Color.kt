package com.unap.aplicaciontareasfinal.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales basados en la nueva estética (Azul/Púrpura neón)
val PrimaryColor = Color(0xFF6C63FF)     // Un azul/púrpura vibrante
val PrimaryVariant = Color(0xFF3A2B8A)   // Un tono más oscuro y serio
val SecondaryColor = Color(0xFF03DAC5)   // Cyan para acentos
val BackgroundDark = Color(0xFF121212)   // Fondo oscuro base
val SurfaceDark = Color(0xFF1E1E1E)      // Superficies (tarjetas, etc.)
val TextWhite = Color(0xFFFFFFFF)        // Texto principal
val TextGray = Color(0xFFB0B0B0)         // Texto secundario

// Nombres genéricos para compatibilidad con MaterialTheme
val PurplePrimary = PrimaryColor
val PurpleSecondary = SecondaryColor
val PurpleTertiary = PrimaryVariant
